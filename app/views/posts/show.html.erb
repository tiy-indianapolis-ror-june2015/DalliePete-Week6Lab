<p id="notice"><%= notice %></p>

<% if current_user %>
<div id="createWhoot" class="panel right">
    <h1>Create a Whoot</h1>
    <p>
    <%= form_for @post do |f| %>
        <%= f.text_area :whoot, class: 'whootText' %>
        <%= f.submit "Whoot!" %>
    <% end %>
    </p>
</div>
<div id="ribbits" class="panel left">w
    <h1>Your Whoot Profile</h1>
    <div class="WhootWrapper">
        <span class="name"><%= @user.username %></span> @<%= @user.username %>
        <p>
        <%= @user.posts.size %> Whoots
        <span class="spacing">XX Followers</span>
        <span class="spacing">XX Following</span>
        </p>
    </div>
</div>

<%= link_to 'Edit', edit_post_path(@post) %> |
<%= link_to 'Back', posts_path %>
<% end %>

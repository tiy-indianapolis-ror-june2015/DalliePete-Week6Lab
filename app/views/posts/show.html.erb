<p id="notice"><%= notice %></p>

<% if current_user %>
<div id="createWhoot" class="panel right">
    <p>
    <%= form_for @post do |f| %>
        <%= f.text_area :whoot, class: 'whootText' %>
        <%= f.submit "Whoot!" %>
    <% end %>
    </p>
</div>

<%= link_to 'Edit', edit_post_path(@post) %> |
<%= link_to 'Back', posts_path %>
<% end %>
